#import "@preview/theorion:0.4.1": *
#import cosmos.clouds: *
#show: show-theorion

== Hom 関手：世界を映すスクリーン

対象 $X$ の「外部との関わり」を数学的にパッケージ化する道具が *Hom 関手* である。これは、ある対象を固定し、それを基準に他の全ての対象を「測る」ための物差しといえる。

#definition(title: "Hom 関手 (Hom Functor)")[
  圏 $cal(C)$ の対象 $X$ を固定したとき：

  *共変Hom関手* $h^X := cal(C)(X, -): cal(C) -> bold("Set")$ は
  - 各対象 $Y$ に集合 $h^X(Y) := cal(C)(X, Y)$（$X$ から $Y$ へのすべての射の集合）を対応
  - 射 $g: Y -> Z$ に写像 $g_*: h^X(Y) -> h^X(Z)$ を対応。$f in cal(C)(X, Y)$ に対して $g_*(f) := g compose f$

  *反変Hom関手* $h_X := cal(C)(-, X): cal(C)^op -> bold("Set")$ は
  - 各対象 $Y$ に集合 $h_X(Y) := cal(C)(Y, X)$ を対応
  - 射 $f: Y -> Z$ に写像 $f^*: h_X(Z) -> h_X(Y)$ を対応。$g in cal(C)(Z, X)$ に対して $f^*(g) := g compose f$
]

#theorem(title: "Hom 関手は関手である")[
  $h^X$ が関手であることを示すには：
  1. *恒等射の保存*：$id_{Y,*}(f) = id_Z compose f = f$ ✓
  2. *合成の保存*：$(g compose h)_*(f) = (g compose h) compose f = g_*(h_*(f))$ ✓

  反変Hom関手についても同様。
]

#important-box(title: "Hom 関手の意味")[
  $h^X(Y)$ は「$X$ から $Y$ への情報の流れ」の全貌を映し出す。これは $X$ という存在を、あらゆる対象 $Y$ というスクリーンに投影して観測しているようなものである。圏論において「$X$ を知る」ことと「関手 $h^X$ を知る」ことは、後に見る米田の補題により、事実上同義となる。
]

== 表現可能関手：抽象への肉付け

任意の関手が Hom 関手の形をしているわけではない。Hom 関手として書ける関手は、抽象的な情報の集まりの中に「源泉となる形」を持っている。

#definition(title: "表現可能関手 (Representable Functor)")[
  関手 $F: bold(C) -> bold("Set")$ が、ある対象 $X in bold(C)$ を用いて $F approx h^X$（自然同型）と表されるとき、$F$ は *$X$ によって表現可能* であるという。このとき $X$ を $F$ の *表現対象* と呼ぶ。
]

#important-box(title: "なぜ『表現』が重要なのか？")[
  表現可能関手であるということは、抽象的な関手 $F$ の背後に、その「源泉」となる具体的な対象 $X$ が隠れていることを意味する。
  例えば、ある複雑な数学的現象（関手）が実は「たった一つの対象 $X$ からの射の集まり」として記述できると分かれば、その現象の解析は対象 $X$ の解析に還元される。これは抽象的な現象に「物理的な実体」を与える操作に他ならない。
]

== Yoneda 補題：情報の圧縮と復元

いよいよ圏論の白眉である米田の補題を導入する。この定理は、自然変換という「巨大な世界の写像」が、実は「一点の像」に凝縮されていることを主張する。

#lemma(title: "米田の補題 (Yoneda Lemma)")[
  圏 $cal(C)$ の対象 $X$ と関手 $F: cal(C) -> bold("Set")$ に対し、次の全単射が存在する：
  $$ op("Nat")(h^X, F) approx F(X) $$

  この対応は、自然変換 $alpha: h^X -> F$ を、その $X$ での成分 $alpha_X(id_X) in F(X)$ へと送る。すなわち：
  - *順方向*：自然変換 $alpha mapsto a_X = alpha_X(id_X) in F(X)$
  - *逆方向*：元 $a in F(X)$ から、成分 $alpha_Y(f) := F(f)(a)$ で定義される自然変換 $alpha$ を復元

  この対応は $X$ と $F$ について自然である。

  *特殊な場合* ($F = h^Y$ として)：
  $$ op("Nat")(h^X, h^Y) approx cal(C)(Y, X) $$
  が得られ、これが米田埋め込み定理の基礎となる。
]

#theorem(title: "米田の補題の詳細な証明")[
  *Step 1: 逆方向の定義と自然性の確認*

  $a in F(X)$ が与えられたとき、自然変換 $alpha^(a): h^X -> F$ を以下のように定義する。
  各対象 $Y in "Ob"(cal(C))$ に対して、$alpha_Y: cal(C)(X, Y) -> F(Y)$ を
  $ alpha_Y(f) := F(f)(a) quad (f in cal(C)(X, Y)) $

  *自然性の確認*：任意の $g: Y -> Z$ に対して、次の図式が可換であることを示す。
  $
         cal(C)(X, Y) & arrow.r^(g_*)  &      cal(C)(X, Z) \
    arrow.b^(alpha_Y) &                & arrow.b^(alpha_Z) \
                 F(Y) & arrow.r^(F(g)) &              F(Z)
  $

  $f in cal(C)(X, Y)$ に対して：
  $
    F(g)(alpha_Y(f)) = F(g)(F(f)(a)) = F(g circle f)(a) = alpha_Z(g circle f) = alpha_Z(g_*(f))
  $

  *Step 2: 単射性*

  自然変換 $alpha, beta: h^X -> F$ が $alpha_X("id"_X) = beta_X("id"_X)$ を満たすと仮定する。任意の $Y$ と $f in cal(C)(X, Y)$ に対して、自然性より：
  $ alpha_Y(f) = F(f)(alpha_X("id"_X)) = F(f)(beta_X("id"_X)) = beta_Y(f) $
  故に $alpha = beta$ である。

  *Step 3: 全射性*

  任意の $a in F(X)$ に対して、上記で定義した $alpha^(a)$ は $alpha^(a)_X("id"_X) = F("id"_X)(a) = a$ を満たす。したがって、この対応は全射である。
]

#important-box(title: "米田補題の驚異的な意味")[
  左辺の $op("Nat")(h^X, F)$ は、圏 $cal(C)$ の全対象とすべての射をわたるような、複雑で高次の構造をすべて含む巨大な集合である。

  それが右辺の $F(X)$ という、単なる一個の集合の要素の情報だけで完全に決定されるという事実は、*情報の極端な圧縮* を表している。

  この圧縮可能性こそが、圏論の「構造的本質主義」の数学的根拠である。
]

== 米田埋め込み：圏の包摂と拡張

#theorem(title: "米田埋め込み定理")[
  対応
  $$ yen: cal(C) -> bold("Set")^(cal(C)^op), quad X mapsto h^X $$
  は、*充満忠実本質的全射* な関手である。すなわち、$yen$ は圏の同値を誘導する。

  射についても：射 $f: X -> Y$ は自然変換 $h^f: h^X arrow.r.double h^Y$ に対応される。具体的には、各 $Z in cal(C)$ に対して $(h^f)_Z: cal(C)(X, Z) -> cal(C)(Y, Z)$ は $(h^f)_Z(g) := f compose g$ で定義される。
]

#corollary(title: "対象の同型性とHom関手")[
  米田埋め込みが充満忠実であるという事実から直接導かれる：
  $$ h^X approx h^Y iff X approx Y $$

  *言い換え*：対象が同型であることと、対応するHom関手が自然同型であることは同義である。
]

#theorem(title: "米田的対象の特徴づけ")[
  従来の集合論では、対象は「要素の集合」として内部的に定義される。米田埋め込みの帰結として、圏論では対象を外部的に特徴づけることができる：

  *対象 $X$ は、すべての射 $f: X -> Y$ （$Y$ が変化）の族 $\{cal(C)(X, Y)\}_{Y in cal(C)}$ によって完全に決定される。*

  これは Leibniz の原理「同じものはすべてを共有する」の圏論的具現化である。
]

== Yoneda 的視点：対象 ＝ 射の振る舞い

米田の補題が示唆する哲学は、現代数学や物理学、さらには計算機科学にまで及ぶ新奇な洞察に満ちている。

=== 1. 存在と観測の同一視
米田の補題の最大の帰結は、「$h^X$ と $h^Y$ が（関手として）同型ならば、$X$ と $Y$ は（対象として）同型である」という事実である。#footnote[
  より正確には、米田埋め込み $yen: bold(C)^op -> bold("Set")^bold(C)$ が充満忠実であることを意味する。
]
これは、*「ある対象の正体を知りたければ、その対象の中身を分解するのではなく、あらゆる対象との関係性（射の振る舞い）を調べ尽くせばよい」* ということを保証する。

=== 2. 外界こそが本質である
集合論的なパラダイムでは、対象は「要素の集まり」として内部から定義された。しかし米田的視点では、対象は「他の全てのものに対する振る舞いのカタログ」として外部から定義される。
- *集合論的視点*: 「私は誰か？」という問いに対し、「私の遺伝子や細胞の構成」で答える。
- *米田的視点*: 「私は誰か？」という問いに対し、「私と関わった他者全員が私に対して抱く反応の総体」で答える。

この視点のスゴい点は、後者（外部からの観測）によって、前者（内部構造）が*完全に、かつ一意に復元できる*と数学的に証明してしまったことにある。

=== 3. 米田埋め込み：圏の宇宙を広げる
米田埋め込み $yen: bold(C) -> bold("Set")^(bold(C)^op)$ により、任意の圏 $bold(C)$ は、より豊かな性質を持つ「関手圏」の中へと送り込まれる。
元の圏 $bold(C)$ では対象が少なすぎて「極限」が存在しない場合でも、関手の宇宙（プリシェイフの圏）の中では、あらゆる構造が自由に構成可能になる。

#proof[
  証明の核心は非常にシンプルかつ美しい。自然変換 $alpha: h^X -> F$ が与えられたとき、各成分 $alpha_Y: h^X(Y) -> F(Y)$ の挙動を追う。

  実は、任意の射 $f: X -> Y$ に対し、自然性の条件から $alpha_Y (f) = F(f)(alpha_X (id_X))$ が導かれる。

  つまり、自然変換 $alpha$ 全体の運命は、たった一つの要素 $alpha_X (id_X) in F(X)$ がどこへ行くかによって完全に決定されているのである。この「一つの種（$id_X$）から全体の森が生成される」ような感覚こそ、構造保存の極致である。
]

#remark()[
  米田の補題は、「真理（対象の本質）」は「それを見る方法（関手）」の中に溶け込んでいると教えてくれる。
  物理学における「観測によって状態が決まる」という考え方や、計算機科学における「インターフェースが型を定義する」という思想は、まさにこの米田的視点の変奏曲と言えるだろう。
]
