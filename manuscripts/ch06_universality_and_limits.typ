#import "@preview/theorion:0.4.1": *
#import cosmos.clouds: *
#show: show-theorion

本章の目的は、圏論における「普遍性（Universality）」という定義手法を理解し、それを具体的な構成法である「極限」へと適用することにある。これは「中身」を使わずに、射の条件だけで対象を唯一（同型を除いて）に特定する極めて強力な方法論である。

== 終対象と始対象：境界の定義

最も単純な普遍性の例は、圏の「端」に位置する対象の定義である。

#definition(title: "終対象と始対象")[
  圏 $cal(C)$ において：

  - 対象 $1 in op("Ob")(cal(C))$ が **終対象** (terminal object) であるとは、任意の対象 $X in op("Ob")(cal(C))$ に対して、$X$ から $1$ への射 $f: X -> 1$ が **一意に存在** することである。
  
  - 対象 $0 in op("Ob")(cal(C))$ が **始対象** (initial object) であるとは、任意の対象 $X in op("Ob")(cal(C))$ に対して、$0$ から $X$ への射 $f: 0 -> X$ が **一意に存在** することである。
]

#theorem(title: "終対象と始対象の一意性（同型意味で）")[
  もし $1, 1' in op("Ob")(cal(C))$ がともに終対象ならば、$1 approx 1'$（唯一の同型を除いて一意）。
  始対象についても同様。
]

#proof[
  $1, 1'$ がともに終対象と仮定。終対象の定義より：
  - $f: 1 -> 1'$ が一意に存在
  - $g: 1' -> 1$ が一意に存在

  $g compose f: 1 -> 1$ を考えると、終対象の定義より、$1$ の恒等射 $id_1$ が唯一なので $g compose f = id_1$。同様に $f compose g = id_{1'}$。故に $f$ は同型。
]

#example(title: "具体的な終対象と始対象")[
  - *Set* 圏：終対象は 1 点集合 $\{ op("pt") \}$、始対象は空集合 $nothing$
  - *Grp* 圏：終対象・始対象はいずれも自明群 $\{ e \}$
  - *Top* 圏：終対象は 1 点空間、始対象は空空間 $nothing$
  - 順序集合：最大元が終対象、最小元が始対象（存在する場合）
]

== 積と余積：情報の統合

複数の対象を統合し、かつ元の情報を損失なく保持する構造を考える。

#definition(title: "積 (Product)")[
  圏 $cal(C)$ において、二つの対象 $A, B in op("Ob")(cal(C))$ の **積** $A times B$ とは、以下の普遍性を満たす、対象 $P$ と射の対 $(pi_A: P -> A, pi_B: P -> B)$ の組である：

  任意の対象 $X$ と射の対 $(f: X -> A, g: X -> B)$ に対して、次の図式
  $$
  X arrow.r^h P arrow.r^(pi_A) A \\
  arrow.d^f arrow.d^(pi_B) arrow.d^g \\
  B
  $$
  を可換にする**唯一の射** $h: X -> P$ が存在する。すなわち、$f = pi_A compose h$ かつ $g = pi_B compose h$。
]

#important-box(title: "普遍性の「唯一性」の意義")[
  この定義における「唯一の射 $h$ の存在」こそが普遍性の核心である。
  
  - **存在性**：その構造が要求された条件を満たすのに十分であることを保証
  - **一意性**：その構造が必要最小限であり、余計な冗長性を含まないことを保証
  
  これにより、$A times B$ は $f$ と $g$ の情報を過不足なく含むことが数学的に保証される。
]

#theorem(title: "積の存在と計算")[
  圏 $cal(C)$ で積が存在する場合、任意の対象 $C$ に対して、関手
  $$ F_C: cal(C) -> cal(C), quad X mapsto C times X $$
  が定義される。さらに、積が存在する圏は **直積閉圏** の構造に向かう。
]

== 普遍性による対象の規定：最適化の原理

本章を通じて現れた「唯一の射が存在する」という条件は、対象を **最適化問題の解** として特徴づけていることを意味する。

#theorem(title: "普遍性としての最適化")[
  圏論における普遍性とは、以下の最適化原理の数学的表現である：
  
  与えられた条件（例：積の射影、プルバックの可換図式）を満たす対象の中で、「最も効率的（最小限）かつ最も豊か（完全）な対象」が、その対象である。
  
  形式的には：条件を満たす任意の対象から、その対象への唯一の遺伝的射が存在する。
]
#important-box(title: "普遍性と実装の独立性")[
  集合論では「$A times B$ とは、順序対 $(a, b)$ の集合である」と具体的に構成された。
  
  圏論では「$A times B$ とは、普遍性を満たす対象である」と定義される。
  
  この違いにより、異なる圏（位相空間の圏、群の圏など）で、同じ「積」の概念を、その圏に固有の方法で（同相写像としての積、直積群など）自動的に実現できるようになる。
  
  *普遍性は、実装依存的な構成から解放し、純粋に構造的な本質のみを取り出す抽象化の力を示している。*
]

== Pullback / Pushout：制約付きの結合

より複雑な「条件付きの統合」として、プルバック（引き戻し）とプッシュアウト（押し出し）を導入する。

#definition(title: "プルバック (Pullback)")[
  共通の標的を持つ二つの射 $f: A -> C$ と $g: B -> C$ に対し、それらの *プルバック* とは、対象 $P$ と射 $p_1: P -> A$、$p_2: P -> B$ の組で、$f circle p_1 = g circle p_2$ を満たし、かつ同様の条件を満たす任意の組 $(Z, q_1, q_2)$ から $P$ への唯一の射が一意に定まるものをいう。
]

#important-box(title: "情報の交差路")[
  プルバックは、集合論における「共通部分」や、データベースにおける「ジョイン」の抽象化である。二つの情報の流れが $C$ で合流する際、その「合致する部分」だけを抽出する操作に対応する。
]

== 極限と余極限：図式の「最良の近似」

これまで個別に見てきた終対象、積、プルバックは、すべて *極限* (Limit) という一つの一般概念に包摂される。

#definition(title: "極限 (Limit)")[
  図式 $D: bold(J) -> bold(C)$ に対し、$D$ の極限とは、対象 $L$ と $L$ から $D$ の各対象への射の族（錐：Cone）であって、他の任意の錐から $L$ への唯一の射が存在するような「最大の錐」のことである。
]

逆に、射の向きをすべて反転させたものを *余極限* (Colimit) と呼ぶ。余積、始対象、プッシュアウトはすべて余極限の具体例である。

== 普遍性という定義様式：なぜ「唯一」なのか

本章を通じて現れた「唯一の射が存在する」という条件は、対象を *「最も効率的かつ非冗長な方法」* で定義していることを意味する。

1. *存在性*: その構造が、要求された条件（例えば積の射影など）を満たすのに十分であることを示す。
2. *一意性*: その構造が必要最小限であり、余計な「ノイズ」を含まないことを示す。

#important-box(title: "普遍性の新奇な意義")[
  従来の数学が「$X$ とは、このような要素を持つ集合である」と内包的に定義していたのに対し、普遍性は「$X$ とは、このような条件を満たす唯一の結節点である」と外延的に定義する。
  
  この方法論により、我々は具体的な構成（集合の直積、あるいは行列の結合）に依存せず、その構造が果たすべき「役割」そのものを直接操作することが可能になる。
]

#tip-box(title: "最小作用の原理との類似")[
  物理学における最小作用の原理が、膨大な経路の中から「真の経路」を一つに特定するように、普遍性は圏の中の無数の対象の中から、特定の構造的役割を担う「最適解」を一つに絞り込む。圏論における構築とは、常にこの最適解を探すプロセスに他ならない。
]