#import "@preview/theorion:0.4.1": *
#import cosmos.clouds: *
#show: show-theorion

== 圏論への入口

圏論は、数学に現れる「対象」と「対象の間の変換」を一つの言語で扱う理論である。初めて学ぶときには、圏論があまりに抽象的に見えるかもしれない。しかし出発点は素朴である。集合の間には写像があり、群の間には準同型があり、位相空間の間には連続写像があり、ベクトル空間の間には線形写像がある。これらはいずれも、ある種類の構造を保ちながら一つの対象から別の対象へ移る方法である。#footnote[圏論は 1945 年の Eilenberg と Mac Lane の論文 "General Theory of Natural Equivalences" を重要な出発点とする。もともとは代数的位相幾何学に現れる「自然な対応」を正確に扱うための言語として整備された。]

圏論の第一歩は、対象そのものの中身を細かく見る代わりに、対象の間にどのような射があるかを見ることである。たとえば群 $G$ を理解するとき、元を一つずつ調べるだけでなく、他の群から $G$ へ入る準同型や、$G$ から他の群へ出ていく準同型を調べる。この外部との関わりの全体が、$G$ の構造を深く反映する。

#important-box(title: "最初の見取り図")[
  圏論でまず見るものは次の三つである。

  1. 対象：集合、群、空間、ベクトル空間など
  2. 射：写像、準同型、連続写像、線形写像など
  3. 合成：射を続けて適用する操作

  そして、この合成が結合律と恒等射に関する規則を満たすとき、そこに圏がある。
]

この章では、圏の定義、基本例、可換図式、具体圏と抽象圏の違いを扱う。第2章以降で射の性質、関手、自然変換へ進むための語彙を準備することが目的である。#footnote[本章ではサイズの問題、すなわち「すべての集合の集まり」は集合か、という基礎論的問題には深入りしない。上巻では直観を優先し、下巻第8章で小圏・局所小圏・宇宙の扱いを改めて整理する。]

== なぜ射から考えるのか

数学の多くの分野では、「構造を保つ写像」が基本的な役割を持つ。

#example(title: "構造を保つ写像")[
  1. 集合 $X, Y$ の間では、任意の写像 $f: X -> Y$ を考える。
  2. 群 $G, H$ の間では、積を保つ写像 $phi: G -> H$、すなわち $phi(x y)=phi(x) phi(y)$ を満たす写像を考える。
  3. 位相空間 $X, Y$ の間では、開集合の逆像が開集合になる写像、すなわち連続写像を考える。
  4. 体 $k$ 上のベクトル空間 $V, W$ の間では、和とスカラー倍を保つ線形写像を考える。
]

これらの例に共通しているのは、ただの対応ではなく、その分野で重要な構造を壊さない対応を選んでいる点である。圏論は、この「構造を保つ対応」を射として抽象化する。#footnote[圏論では、射は必ずしも集合間の写像である必要はない。順序集合を圏と見た場合の射は「大小関係」を表し、モノイドを一対象圏と見た場合の射はモノイドの元を表す。この点が、圏論を単なる写像論以上のものにしている。]

射を中心に置く利点は、異なる分野の似た構成を同じ形式で書けることである。集合の直積、群の直積、位相空間の直積は、それぞれ作り方は違うが、ある同じ普遍的性質を満たす。この性質は第6章で扱うが、その準備として、本章ではまず「対象と射の体系」を定義する。

== 圏の定義

#definition(title: "圏")[
  *圏* $cal(C)$ は、次のデータからなる。

  1. 対象の集まり $op("Ob")(cal(C))$
  2. 任意の二対象 $X, Y$ に対する射の集まり $cal(C)(X,Y)$
  3. 任意の三対象 $X, Y, Z$ に対する合成写像
     $
       cal(C)(Y,Z) times cal(C)(X,Y) -> cal(C)(X,Z)
     $
     ただし $g: Y -> Z$ と $f: X -> Y$ の合成を $g compose f: X -> Z$ と書く
  4. 各対象 $X$ に対する恒等射 $id_X: X -> X$

  これらは次の公理を満たす。

  1. *結合律*：$f: W -> X$, $g: X -> Y$, $h: Y -> Z$ なら
     $
       h compose (g compose f) = (h compose g) compose f
     $
  2. *左単位律*：$f: X -> Y$ なら
     $
       id_Y compose f = f
     $
  3. *右単位律*：$f: X -> Y$ なら
     $
       f compose id_X = f
     $
]

記号 $f: X -> Y$ は、「$f$ は $X$ から $Y$ への射である」という意味である。より形式的には $f in cal(C)(X,Y)$ と書ける。集合論で写像 $f: X -> Y$ と書くときと似ているが、圏論では $f$ が必ず集合の写像であるとは限らない。#footnote[Hom という語は homomorphism に由来する。$cal(C)(X,Y)$ は $op("Hom")_cal(C)(X,Y)$ とも書かれる。本書では文脈に応じて $cal(C)(X,Y)$ と $op("Hom")_cal(C)(X,Y)$ を併用する。]

この定義で重要なのは、対象の内部構造が一切指定されていないことである。対象は集合でもよいし、群でもよいし、空間でもよい。あるいは、何かの状態、命題、証明、計算過程でもよい。必要なのは、射と合成と恒等射があり、それらが上の二つの規則に従うことである。

#remark(title: "結合律の意味")[
  結合律は、三つの射を続けて合成するとき、どこから先に合成しても結果が同じであることを保証する。したがって
  $
    h compose g compose f
  $
  と括弧を省略して書ける。
]

#remark(title: "恒等射の意味")[
  恒等射 $id_X$ は、対象 $X$ に対する「何もしない射」である。写像の圏では恒等写像、群の圏では恒等準同型、順序集合を圏と見る場合には反射律 $x <= x$ に対応する。
]

#lemma(title: "恒等射の一意性")[
  圏 $cal(C)$ において、各対象 $X$ の恒等射は一意である。
]

#proof[
  $e: X -> X$ と $e': X -> X$ がともに $X$ の恒等射の役割を果たすとする。$e$ が左単位として働くので $e compose e'=e'$ である。一方、$e'$ が右単位として働くので $e compose e'=e$ である。したがって $e=e'$。
]

この補題は小さいが、重要である。圏の定義では各対象に恒等射を「指定する」と言ったが、公理を満たすものは一つしかない。したがって、いったん圏が与えられれば $id_X$ は曖昧でない。

== 最初の具体例

定義だけを眺めると圏は遠く見える。そこで、よく知っている数学的対象がどのように圏をなすかを確認する。

#example(title: "集合の圏 Set")[
  $bold("Set")$ を次のように定める。

  - 対象：集合
  - 射：集合間の写像
  - 合成：通常の写像の合成
  - 恒等射：恒等写像

  写像の合成は結合的であり、恒等写像は合成に関する単位として働く。したがって $bold("Set")$ は圏である。
]

$bold("Set")$ は最も基本的な圏である。多くの具体圏は、対象が集合に追加構造を持ったもので、射がその構造を保つ写像である。

#example(title: "群の圏 Grp")[
  $bold("Grp")$ を次のように定める。

  - 対象：群
  - 射：群準同型
  - 合成：写像としての合成
  - 恒等射：恒等準同型

  群準同型 $phi: G -> H$ と $psi: H -> K$ について、
  $
    (psi compose phi)(x y)
      = psi(phi(x y))
      = psi(phi(x) phi(y))
      = psi(phi(x)) psi(phi(y))
  $
  であるから、合成 $psi compose phi$ も群準同型である。
]

#footnote[群準同型は単位元と逆元も保存する。単位元の保存を準同型の定義に含める流儀と、積の保存から導く流儀があるが、本書では単位元を保つ準同型を標準とする。]

#example(title: "位相空間の圏 Top")[
  $bold("Top")$ を次のように定める。

  - 対象：位相空間
  - 射：連続写像
  - 合成：写像としての合成
  - 恒等射：恒等写像

  連続写像の合成は連続である。実際、$f: X -> Y$ と $g: Y -> Z$ が連続なら、$Z$ の開集合 $U$ に対し
  $
    (g compose f)^(-1)(U)=f^(-1)(g^(-1)(U))
  $
  であり、右辺は開集合である。
]

#example(title: "ベクトル空間の圏 Vect")[
  体 $k$ を固定し、$bold("Vect")_k$ を次のように定める。

  - 対象：$k$-ベクトル空間
  - 射：$k$-線形写像
  - 合成：線形写像の合成
  - 恒等射：恒等線形写像

  線形写像の合成は再び線形である。つまり、和とスカラー倍を保つという性質は合成で壊れない。
]

これらの例では、射はすべて集合間の写像である。しかし圏論の射はそれに限られない。次の二つの例は、圏の定義がどれほど柔軟であるかを示す。

== 抽象的な例

#example(title: "順序集合から作る圏")[
  順序集合 $(P, <=)$ から圏を作る。対象は $P$ の元である。二つの対象 $x,y$ に対し、$x <= y$ のときただ一つの射 $x -> y$ があり、そうでないとき射はないと定める。

  恒等射は反射律 $x <= x$ に対応する。合成は推移律
  $
    x <= y " かつ " y <= z " なら " x <= z
  $
  に対応する。したがって順序集合は圏と見なせる。
]

#footnote[このように Hom 集合が高々一元集合である圏を thin category と呼ぶことがある。順序集合、より正確には前順序集合は thin category の典型例である。]

順序集合の例では、射は「写像」ではなく「比較可能性」である。$x -> y$ という射は、$x$ から $y$ へ何かを送る操作というより、$x$ が $y$ 以下であるという事実を表す。

#example(title: "モノイドから作る一対象圏")[
  モノイド $(M, *, e)$ から一つの対象 $*$ だけを持つ圏を作る。射 $* -> *$ は $M$ の元とし、射の合成をモノイドの積で定める。恒等射は単位元 $e$ である。

  モノイドの結合律は圏の結合律に対応し、モノイドの単位元は圏の恒等射に対応する。
]

この例は重要である。圏は「対象がたくさんある構造」とは限らない。一つの対象しかなくても、射が豊かな代数構造を持てば非自明な圏になる。#footnote[逆に、一対象圏はモノイドと同じデータを持つ。さらにすべての射が可逆である一対象圏は群と同じデータを持つ。]

== 具体圏と抽象圏

これまでの例は、大きく二つに分けられる。対象が集合に何らかの構造を加えたもので、射が構造保存写像である圏を、直観的には具体圏と呼ぶ。一方、順序集合やモノイドから作る圏のように、射を写像として考える必要がない圏もある。

#figure(
  caption: [具体圏と抽象圏の比較],
  placement: top,
  table(
    columns: (8em, auto, auto),
    align: (left, left, left),
    inset: (x: 8pt, y: 4pt),
    stroke: (x, y) => if y <= 1 { (top: 0.5pt) },
    fill: (x, y) => if y > 0 and calc.rem(y, 2) == 0 { rgb("#efefef") },

    table.header[*観点*][*具体圏*][*抽象圏*],

    [対象],
    [集合に構造を加えたもの],
    [内部を指定しない対象],

    [射],
    [構造を保つ写像],
    [公理を満たす矢印],

    [例],
    [$bold("Set"), bold("Grp"), bold("Top"), bold("Vect")_k$],
    [順序圏、一対象圏],

    [考え方],
    [要素でも考えられる],
    [射の関係で考える],
  )
)

ただし、この区別は絶対的なものではない。多くの抽象圏も、適切な仕方で集合の圏へ写すことで具体的に表現できる場合がある。一方で、圏論の本質は、具体的に表現できるかどうかではなく、射と合成によって何が言えるかにある。

== 可換図式

圏論では、射の等式を図で表すことが多い。これを可換図式という。

#definition(title: "可換図式")[
  図式が *可換* であるとは、同じ始点から同じ終点へ向かう任意の経路が、合成した射として等しいことをいう。
]

たとえば、射 $f: A -> B$, $g: B -> D$, $h: A -> C$, $k: C -> D$ があるとき、
$
  g compose f = k compose h
$
が成り立つことを、正方形の可換性として表す。

#example(title: "写像の可換図式")[
  集合の写像
  $
    f: A -> B, quad h: A -> C, quad g: B -> D, quad k: C -> D
  $
  を考える。任意の $a in A$ について
  $
    g(f(a)) = k(h(a))
  $
  が成り立つなら、図式は可換である。つまり、$A$ から $D$ へ行く二つの方法が同じ結果を与える。
]

#tip-box(title: "可換図式の読み方")[
  可換図式を読むときは、次の順に確認するとよい。

  1. 対象を確認する。
  2. 矢印の向きを確認する。
  3. 同じ始点と終点を持つ経路を探す。
  4. それらの経路に対応する合成射が等しいと読む。
]

可換図式は、圏論における文章であり、計算式であり、図でもある。第4章以降で自然変換を学ぶと、可換図式は単なる補助記法ではなく、概念そのものを定義する道具になる。

== 対象を点として見る

圏論の特徴は、対象の内部にある要素を直接使わずに議論できることである。これは「対象を点として見る」と言い換えられる。

#important-box(title: "視点の転換")[
  従来の視点では、対象を要素と演算から理解する。

  圏論的な視点では、対象を他の対象との射の関係から理解する。
]

この視点は、最初は不自然に感じられるかもしれない。たとえば集合 $X$ を調べるなら、普通は $X$ の元を見る。しかし圏論では、他の集合から $X$ への写像や、$X$ から他の集合への写像を見る。実は、十分多くの射を見れば、対象の性質はかなりの程度まで復元できる。#footnote[この考え方は第5章の Hom 関手と米田の補題で精密化される。米田の補題は、対象はその対象へ向かう射の全体によって完全に特徴づけられる、という圏論的思想を代表する定理である。]

#example(title: "一点集合による元の表現")[
  $bold("Set")$ では、集合 $X$ の元 $x in X$ は、一点集合 $1$ から $X$ への写像
  $
    x: 1 -> X
  $
  と同一視できる。一点集合の唯一の元を $x$ に送る写像が、元 $x$ を表すのである。
]

この例は、圏論で「元」を完全に捨てるわけではないことを示している。むしろ元でさえ、射として言い換える。これにより、要素を持たないように見える対象にも同じ言語を適用できる。

== 合成という基本操作

射は単独で存在するだけではない。合成できることが本質である。

#example(title: "三つの合成")[
  1. 写像 $f: X -> Y$ と $g: Y -> Z$ の合成 $g compose f$ は、$x$ をまず $f(x)$ に送り、次に $g(f(x))$ に送る。
  2. 群準同型 $G -> H -> K$ の合成は、群構造を保つ写像である。
  3. 連続写像 $X -> Y -> Z$ の合成は、開集合の逆像を二段階で取るので連続である。
]

圏の公理は、このような合成が安定していることを抽象化している。結合律がなければ、長い合成を書くたびに括弧を厳密に追わなければならない。恒等射がなければ、「何もしない操作」を合成の単位として扱えない。

== 圏論が統一するもの

圏論の強みは、似た性質を一つの言葉で扱えることである。たとえば「積」は、集合、群、位相空間、ベクトル空間でそれぞれ異なる構成を持つが、圏論では同じ普遍性によって定義される。

#figure(
  caption: [具体的構成と圏論的概念],
  placement: top,
  table(
    columns: (8em, auto, auto),
    align: (left, left, left),
    inset: (x: 8pt, y: 4pt),
    stroke: (x, y) => if y <= 1 { (top: 0.5pt) },
    fill: (x, y) => if y > 0 and calc.rem(y, 2) == 0 { rgb("#efefef") },

    table.header[*具体的な構成*][*例*][*圏論的な見方*],

    [終わりの対象],
    [一点集合、自明群],
    [終対象],

    [始まりの対象],
    [空集合、自由な空生成対象],
    [始対象],

    [直積],
    [集合の直積、群の直積],
    [積],

    [貼り合わせ],
    [位相空間の商、代数的な余商],
    [余極限],

    [構造保存],
    [準同型、連続写像、線形写像],
    [射],
  )
)

これらの概念は本章では名前だけにとどめる。第6章で普遍性と極限を学ぶと、表の右列が正確な定義として現れる。

== 本書での学び方

圏論は、最初からすべてを直観的に理解しようとすると難しい。むしろ、いくつかの基本例に戻りながら、新しい定義を少しずつ読むのがよい。

#tip-box(title: "初学者のための読み方")[
  1. 新しい定義を読んだら、まず $bold("Set")$ で何を意味するか確認する。
  2. 次に $bold("Grp")$ や $bold("Top")$ で同じ言葉がどう変わるか見る。
  3. 順序集合や一対象圏の例で、射が写像とは限らないことを思い出す。
  4. 可換図式は、必ず経路ごとの合成として読み下す。
]

この章で覚えるべきことは多くない。圏とは、対象、射、合成、恒等射からなる構造である。射は合成でき、合成は結合的で、恒等射は何もしない射として働く。この単純な枠組みが、以後のすべての章の土台になる。

#remark[
  圏論は「抽象的すぎる」と言われることがある。しかし抽象化の目的は、具体性を捨てることではない。複数の具体例に共通する骨格を取り出し、その骨格だけでどこまで議論できるかを明らかにすることである。
]

次章では、射そのものの性質を調べる。単射・全射に対応するモノ射・エピ射、同型射、部分対象などを導入し、圏論がどのように通常の数学的概念を言い換えるかを見ていく。
