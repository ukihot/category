#import "@preview/theorion:0.4.1": *
#import cosmos.clouds: *
#import "@preview/codly:1.3.0": *
#import "@preview/codly-languages:0.1.1": *
#import "@preview/fletcher:0.5.8" as fletcher: diagram, edge, node
#show: show-theorion
#show: codly-init.with()
#codly(languages: codly-languages)

層理論は、局所的に与えられたデータを大域的な対象へ貼り合わせる理論である。連続関数、正則関数、微分形式、局所解、ベクトル束の切断など、幾何学で現れる対象はしばしば局所的にはよく理解できるが、大域的には非自明な障害を持つ。層は、この「局所から大域へ」の移行を圏論的に定式化する。

トポス理論は、層の圏が持つ性質を抽象化したものである。トポスは集合の圏に似た振る舞いをするが、その内部論理は必ずしも古典論理ではない。したがってトポスは、一般化された空間であると同時に、一般化された集合宇宙でもある。

== 前層

位相空間 $X$ を固定する。$op("Open")(X)$ を、$X$ の開集合を対象、包含 $V subset.eq U$ を射 $V -> U$ とする圏とする。

#definition(title: "前層")[
  $X$ 上の *集合値前層* とは、反変関手
  $
    cal(F): op("Open")(X)^op -> bold("Set")
  $
  である。開集合 $U$ に対する集合 $cal(F)(U)$ の元を、$U$ 上の *切断* という。包含 $V subset.eq U$ に対応する写像
  $
    rho_(U,V): cal(F)(U) -> cal(F)(V)
  $
  を *制限写像* という。
]

関手性は
$
  rho_(U,U)=id, quad rho_(V,W) compose rho_(U,V)=rho_(U,W)
$
を意味する。つまり、大きな開集合上のデータは小さな開集合へ制限でき、その制限は段階を踏んでも一度に行っても同じである。

#example(title: "前層の例")[
  1. $cal(C)^0(U)$ を $U$ 上の連続実数値関数全体とすれば、これは前層である。
  2. $cal(O)(U)$ を複素多様体の開集合 $U$ 上の正則関数全体とすれば、これは前層である。
  3. 固定集合 $A$ に対して $U |-> A$ と置くと定数前層が得られる。ただしこれは一般には層ではない。
]

== 層条件

前層が層であるとは、局所データが一意に貼り合わさることをいう。

#definition(title: "層")[
  前層 $cal(F)$ が *層* であるとは、任意の開集合 $U$ と開被覆 $U = union.big_i U_i$ に対して次を満たすことである。

  1. *局所性*：$s,t in cal(F)(U)$ がすべての $i$ で $s|_(U_i)=t|_(U_i)$ を満たすなら、$s=t$。
  2. *貼り合わせ*：各 $i$ に $s_i in cal(F)(U_i)$ が与えられ、すべての $i,j$ について
    $
      s_i|_(U_i inter U_j)=s_j|_(U_i inter U_j)
    $
    が成り立つなら、ある一意な $s in cal(F)(U)$ が存在して $s|_(U_i)=s_i$ となる。
]

この条件は、次の図式が等化子であることとして表せる。

#align(center)[
  #diagram(
    cell-size: 22mm,
    $
      cal(F)(U) edge(->) & product_i cal(F)(U_i) edge("r", rho_1, ->) edge("r", rho_2, ->) & product_(i,j) cal(F)(U_i inter U_j)
    $,
  )
]

#example(title: "層と非層")[
  連続関数、可微分関数、正則関数、微分形式は層をなす。一方、定数前層 $U |-> A$ は、開集合が複数の連結成分を持つ場合に貼り合わせ条件を満たさないことがある。これを層化すると、局所定数関数の層が得られる。
]

== 茎と層化

層は各点の近傍での振る舞いからも調べられる。

#definition(title: "茎")[
  前層 $cal(F)$ と点 $x in X$ に対し、$x$ での *茎* は
  $
    cal(F)_x = op("colim")_(x in U) cal(F)(U)
  $
  と定義される。元は、$x$ のある近傍で定義された切断の芽である。
]

#theorem(title: "層の射の茎による判定")[
  層の射 $phi: cal(F) -> cal(G)$ が同型であることと、すべての点 $x in X$ で誘導される射
  $
    phi_x: cal(F)_x -> cal(G)_x
  $
  が同型であることは同値である。
]

#definition(title: "層化")[
  任意の前層 $cal(F)$ に対し、層 $cal(F)^a$ と前層の射 $cal(F) -> cal(F)^a$ が存在し、任意の層 $cal(G)$ について
  $
    op("Hom")_(bold("Sh")(X))(cal(F)^a, cal(G))
    approx
    op("Hom")_(bold("PSh")(X))(cal(F), cal(G))
  $
  を満たす。$cal(F)^a$ を $cal(F)$ の *層化* という。
]

層化は、前層から局所性と貼り合わせを強制して得られる最良の層である。圏論的には、層化関手は包含 $bold("Sh")(X) -> bold("PSh")(X)$ の左随伴である。

#align(center, fletcher.diagram(
  spacing: 48pt,
  node((0, 1), $bold("PSh")(X)$),
  node((1, 1), $bold("Sh")(X)$),
  node((0, 0), $cal(F)$),
  node((1, 0), $cal(F)^a$),
  edge((0, 1), (1, 1), $(-)^a$, "->"),
  edge((1, 1), (0, 1), $i$, "hook-->"),
  edge((0, 0), (1, 0), $eta_cal(F)$, "->"),
))

== 層の圏

#theorem(title: "層の圏の基本性質")[
  位相空間 $X$ 上の集合値層の圏 $bold("Sh")(X)$ は有限極限と任意の余極限を持つ。さらに指数対象と部分対象分類子を持ち、初等トポスである。
]

#remark[
  極限は前層として計算してから層条件を確認すればよい場合が多い。余極限は前層として計算した後、必要に応じて層化する。この「前層で計算し、層化する」という手順は層論の基本技法である。
]

アーベル群値層の圏 $bold("Sh")(X, bold("Ab"))$ はアーベル圏である。したがって第10章のホモロジー代数を層に適用できる。

== サイト

Grothendieck の洞察は、層を定義するために必ずしも位相空間の開集合を必要としないという点にあった。必要なのは、「どの射の族を被覆と見なすか」という情報である。

#definition(title: "篩")[
  圏 $cal(C)$ の対象 $U$ 上の *篩* $S$ とは、$U$ へ入る射の集まりであって、$f: V -> U$ が $S$ に属し、$g: W -> V$ が任意の射なら、$f g: W -> U$ も $S$ に属するものをいう。
]

#definition(title: "Grothendieck 位相")[
  圏 $cal(C)$ 上の *Grothendieck 位相* $J$ は、各対象 $U$ に被覆篩の集合 $J(U)$ を割り当て、次を満たす。

  1. 最大篩は被覆である。
  2. 被覆篩は引き戻しで被覆篩に移る。
  3. 被覆篩の上で局所的に被覆である篩は被覆である。
]

組 $(cal(C),J)$ を *サイト* という。サイト上の層は、位相空間上の層条件を篩の言葉で一般化して定義される。

#example(title: "重要なサイト")[
  1. 位相空間 $X$ の開集合の圏と通常の開被覆。
  2. スキーム $X$ の Zariski サイト。
  3. スキーム $X$ の etale サイト。
  4. 滑らかな多様体の圏に開被覆を入れたサイト。
]

== Grothendieck トポス

#definition(title: "Grothendieck トポス")[
  ある小さなサイト $(cal(C),J)$ 上の集合値層の圏と同値な圏を *Grothendieck トポス* という。
]

Grothendieck トポスは、空間から生じる層の圏を大きく一般化する。位相空間がなくても、サイトがあれば「局所性」と「貼り合わせ」を語ることができる。

#theorem(title: "Giraud の定理")[
  圏 $cal(E)$ が Grothendieck トポスであるための条件は、有限極限、小余極限、生成集合、余極限の普遍性、および有効同値関係に関するいくつかの公理により特徴づけられる。
]

#remark[
  Giraud の定理は、Grothendieck トポスを「サイト上の層の圏」としてではなく、内在的な圏論的性質によって認識できることを示す。
]

== 初等トポス

Grothendieck トポスより公理を弱め、集合の圏に似た論理的構造を抽出したものが初等トポスである。

#definition(title: "初等トポス")[
  圏 $cal(E)$ が *初等トポス* であるとは、次を満たすことである。

  1. 有限極限を持つ。
  2. 指数対象を持つ。
  3. 部分対象分類子 $Omega$ を持つ。
]

#definition(title: "部分対象分類子")[
  *部分対象分類子* とは、対象 $Omega$ と射 $"true": 1 -> Omega$ であって、任意のモノ射 $m: A -> B$ に対し、一意の射 $chi_m: B -> Omega$ が存在し、次の正方形が引き戻しになるものをいう。

  #align(center)[
    #diagram(
      cell-size: 18mm,
      $
        A edge(->) edge("d", m, "hook-->") & 1 edge("d", "true", ->) \
                         B edge(chi_m, ->) & Omega
      $,
    )
  ]
]

#example[
  $bold("Set")$ では $Omega={0,1}$ であり、$chi_m$ は通常の特性関数である。層のトポス $bold("Sh")(X)$ では、$Omega$ は開集合の層として現れ、真理値は単なる真偽ではなく局所的な真理値を持つ。
]

== 幾何学的射

トポス間の正しい射は、単なる関手ではなく随伴対である。

#definition(title: "幾何学的射")[
  トポス $cal(E), cal(F)$ の間の *幾何学的射* $f: cal(E) -> cal(F)$ とは、随伴対
  $
    f^*: cal(F) -> cal(E) tack f_*: cal(E) -> cal(F)
  $
  であって、逆像関手 $f^*$ が有限極限を保存するものをいう。
]

連続写像 $f: X -> Y$ は、層の逆像・直像により幾何学的射
$
  bold("Sh")(X) -> bold("Sh")(Y)
$
を誘導する。このため、トポスは空間の一般化として振る舞う。

#align(center, fletcher.diagram(
  spacing: 50pt,
  node((0, 1), $X$),
  node((1, 1), $Y$),
  node((0, 0), $bold("Sh")(X)$),
  node((1, 0), $bold("Sh")(Y)$),
  edge((0, 1), (1, 1), $f$, "->"),
  edge((0, 0), (1, 0), $f_*$, "->"),
  edge((1, 0), (0, 0), $f^*$, "->"),
))

#definition(title: "点")[
  トポス $cal(E)$ の *点* とは、幾何学的射
  $
    bold("Set") -> cal(E)
  $
  のことである。
]

位相空間 $X$ の点 $x$ は、茎を取る関手により $bold("Set") -> bold("Sh")(X)$ という点を与える。しかし一般のトポスは十分な点を持たないこともある。この事実は、トポスが通常の空間より柔軟な対象であることを示している。

== 内部論理

初等トポスでは、対象を型、射を項、部分対象を述語として読むことで、内部で論理を展開できる。

#definition(title: "内部論理の対応")[
  トポス $cal(E)$ において、次の対応が成り立つ。

  - 対象：型
  - 射 $X -> Y$：項または関数
  - 部分対象 $A -> X$：$X$ 上の述語
  - $Omega$：真理値対象
  - 有限極限：論理積と等式
  - 指数対象：含意と関数型
]

多くのトポスの内部論理は直観主義論理であり、排中律や選択公理は一般には成り立たない。これは欠陥ではなく、局所的・構成的な数学を自然に表すための特徴である。

== 層コホモロジー

層理論の力は、局所データの貼り合わせが失敗する度合いを測れる点にある。

#definition(title: "層コホモロジー")[
  位相空間 $X$ 上のアーベル群値層 $cal(F)$ に対し、大域切断関手
  $
    Gamma(X, -): bold("Sh")(X,bold("Ab")) -> bold("Ab")
  $
  の右導来関手を
  $
    H^n(X,cal(F)) = R^n Gamma(X, cal(F))
  $
  と書き、$cal(F)$ の *層コホモロジー* という。
]

#example[
  コンパクトリーマン面 $X$ 上の正則関数の層 $cal(O)$ について、$H^1(X,cal(O))$ は $X$ の複素解析的な大域構造を反映する。局所的には正則関数を貼り合わせられても、大域的な障害が一次コホモロジーに現れる。
]

== まとめ

層は局所データと大域データの関係を表す。サイトは「局所」の概念を圏論的に一般化し、Grothendieck トポスはその上の層の圏として得られる。初等トポスは集合の圏に似た論理的宇宙であり、内部論理を通じて数学そのものを展開できる。

次章では、自然変換をさらに高次の射として扱い、圏そのものを高次化する。層とトポスの理論は、高次圏論へ進むと $infinity$-トポスとして再び現れる。
