#import "@preview/theorion:0.4.1": *
#import cosmos.clouds: *
#import "@preview/fletcher:0.5.8" as fletcher: diagram, edge, node
#show: show-theorion

上巻では、対象と射、関手、自然変換、極限、随伴という圏論の基本文法を、できるかぎり「中身を見ない」形で展開した。しかし圏論を基礎言語として用いる段階では、圏そのものをどの集合論的階層に置くか、また圏の内部にどのような構造を許すかを明確にしなければならない。本章の目的は、圏論の実践に不可欠な二つの基礎技法、すなわち *サイズ管理* と *内部構造の抽出* を整理することである。

ここで扱う議論は形式的に見えるが、単なる細部ではない。米田埋め込み、関手圏、随伴関手定理、層の圏、トポス、さらには高次圏論は、いずれも「どの集まりを集合として扱っているのか」という問いに依存している。したがって本章は、下巻全体の集合論的な土台を与える章である。

== サイズの問題

圏 $bold("Set")$ は、数学の最も基本的な例である。対象は集合、射は写像である。しかし「すべての集合の集まり」は集合ではない。したがって $bold("Set")$ の対象全体を、通常の意味で一つの集合として扱うことはできない。

#definition(title: "クラスと真のクラス")[
  集合論的基礎を固定したとき、対象として扱える集まりを *クラス* と呼ぶ。集合ではないクラスを *真のクラス* という。
]

#remark[
  圏論で問題になるのは、多くの場合「射の集まり」よりも「対象の集まり」である。たとえば $bold("Set")$ では、任意の二つの集合 $X, Y$ に対する $bold("Set")(X, Y)$ は集合である。しかし対象全体 $op("ob")(bold("Set"))$ は真のクラスである。
]

この区別を無視すると、「すべての圏の圏」「すべての関手の圏」「すべての自然変換の圏」などを無制限に作ることになり、Russell 型の矛盾に近い問題を招く。圏論の豊かさは、適切な制限を設けることで初めて厳密に扱える。

== 小圏と局所小圏

#definition(title: "小圏と局所小圏")[
  圏 $cal(C)$ について、次の用語を用いる。

  - $cal(C)$ が *小さい* とは、$op("ob")(cal(C))$ が集合であり、かつ各 $X, Y$ に対して $cal(C)(X, Y)$ が集合であることをいう。
  - $cal(C)$ が *局所小さい* とは、各 $X, Y$ に対して $cal(C)(X, Y)$ が集合であることをいう。
  - $cal(C)$ が *本質的に小さい* とは、$cal(C)$ と同値な小圏が存在することをいう。
]

#example(title: "基本例")[
  1. 有限集合と写像からなる圏 $bold("FinSet")$ は小圏ではないように見えるが、各濃度ごとに代表を一つ取れば同値な小圏が得られる。したがって本質的に小さい。
  2. $bold("Set")$, $bold("Grp")$, $bold("Top")$ は局所小さいが、通常は小さくない。
  3. 一つのモノイド $M$ は、一対象小圏と見なせる。この圏は $M$ が集合なら小さい。
]

#important-box(title: "局所小ささの役割")[
  局所小さい圏では Hom が集合であるため、Hom 関手
  $ cal(C)(A, -): cal(C) -> bold("Set") $
  を定義できる。米田の補題、表現可能関手、随伴の Hom 同型は、いずれもこの条件のもとで自然に述べられる。
]

局所小ささは、対象全体を集合として数え上げることを要求しない。これは実践上重要である。数学で現れる多くの圏は対象が大きいが、二対象間の射は集合として十分に制御されている。

== 宇宙による相対化

サイズを厳密に扱う標準的方法の一つは、Grothendieck 宇宙を固定することである。

#definition(title: "Grothendieck 宇宙")[
  集合 $U$ が *Grothendieck 宇宙* であるとは、次を満たすことである。

  1. $x in y in U$ ならば $x in U$
  2. $x, y in U$ ならば $\{x, y\} in U$
  3. $x in U$ ならば $cal(P)(x) in U$
  4. $I in U$ かつ各 $i in I$ について $x_i in U$ ならば $union.big_(i in I) x_i in U$
]

宇宙 $U$ を固定すると、$U$ の要素である集合を *$U$-小さい集合* と呼ぶ。すると $U$-小さい集合を対象とする圏 $bold("Set")_U$ は、より大きな宇宙 $V$ の中では小圏として扱える。

#remark(title: "相対的な小ささ")[
  「小さい」という語は絶対的ではない。ある宇宙 $U$ に対して大きい圏が、より大きな宇宙 $V$ に対しては小さい圏として扱われることがある。圏論では、多くの主張がこの相対化に対して不変であるように定式化される。
]

本書では、必要に応じて十分大きな宇宙を固定しているものとして議論する。ただし、定理の前提に小ささが本質的に関わる場合には明示する。

== 部分圏と充満性

圏の内部構造を調べる最初の操作は、対象や射を制限することである。

#definition(title: "部分圏")[
  圏 $cal(D)$ が圏 $cal(C)$ の *部分圏* であるとは、次を満たすことである。

  1. $op("ob")(cal(D)) subset.eq op("ob")(cal(C))$
  2. 各 $X, Y in op("ob")(cal(D))$ について $cal(D)(X, Y) subset.eq cal(C)(X, Y)$
  3. 合成と恒等射は $cal(C)$ のものの制限である
]

#definition(title: "充満部分圏と忠実な埋め込み")[
  部分圏 $cal(D) subset.eq cal(C)$ が *充満* であるとは、任意の $X, Y in cal(D)$ に対して
  $ cal(D)(X, Y) = cal(C)(X, Y) $
  が成り立つことをいう。

  関手 $I: cal(D) -> cal(C)$ が忠実かつ対象上単射であるとき、$cal(D)$ は $cal(C)$ に埋め込まれていると考える。さらに $I$ が充満なら、これは充満部分圏と同じ情報を持つ。
]

#example[
  有限アーベル群の圏 $bold("FinAb")$ は、アーベル群の圏 $bold("Ab")$ の充満部分圏である。対象を有限なものに制限しているが、有限アーベル群の間の準同型はすべてそのまま採用している。
]

充満性は、単なる便利な語ではない。対象のクラスを制限しても射の構造を失わない場合、その部分圏での計算は元の圏の計算と完全に整合する。

== 骨格と本質的小ささ

圏論では、同型な対象を区別しない視点が自然である。これを集合論的に制御する道具が骨格である。

#definition(title: "骨格")[
  圏 $cal(C)$ の *骨格* とは、$cal(C)$ の充満部分圏
  $op("sk")(cal(C))$
  であって、$cal(C)$ の各対象が
  $op("sk")(cal(C))$
  のただ一つの対象と同型になるものをいう。
]

#theorem(title: "骨格と本質的小ささ")[
  圏 $cal(C)$ が本質的に小さいことと、$cal(C)$ が小さい骨格を持つことは同値である。
]

#proof[
  小さい圏 $cal(D)$ と同値 $F: cal(D) -> cal(C)$ が与えられたとする。$cal(C)$ の各同型類から $F$ の像に同型な代表を選ぶことで小さい骨格が得られる。逆に、小さい骨格の包含は本質的全射かつ充満忠実であるから同値を与える。
]

骨格は「同型な対象を一つにまとめる」操作である。ただし、骨格の選択は一般に標準的ではない。したがって構成の自然性を重視する場面では、骨格そのものよりも同値不変な主張を用いるのが望ましい。

== 関手圏とサイズ

圏 $cal(C)$ と $cal(D)$ が与えられると、関手と自然変換からなる関手圏 $[cal(C), cal(D)]$ を考えたくなる。しかしこれは常に無条件で小さな圏になるわけではない。

#theorem(title: "関手圏の局所小ささ")[
  $cal(C)$ が小圏で、$cal(D)$ が局所小さい圏なら、関手圏 $[cal(C), cal(D)]$ は局所小さい。
]

#proof[
  関手 $F, G: cal(C) -> cal(D)$ の間の自然変換は、各対象 $C in cal(C)$ に対する射
  $ alpha_C: F(C) -> G(C) $
  の族であり、任意の射 $f: C -> C'$ に対して次の自然性四角形を可換にするものとして定義される。

  #align(center)[
    #diagram(
      cell-size: 18mm,
      $
        F(C) edge(F(f), ->) edge("d", alpha_C, ->) & F(C') edge("d", alpha_(C'), ->) \
                               G(C) edge(G(f), ->) & G(C')
      $,
    )
  ]

  $cal(C)$ の対象が集合であり、各 Hom が集合であるため、自然変換全体は集合の積の部分集合として表される。
]

#remark[
  この定理は前層圏 $[cal(C)^op, bold("Set")]$ の基礎を支えている。前層圏を扱うには、通常 $cal(C)$ を小圏として仮定する。
]

== Cat の位置づけ

小圏を対象、関手を射とする圏を $bold("Cat")$ と書く。

#definition(title: "圏 Cat")[
  $bold("Cat")$ は次のデータからなる圏である。

  - 対象：小圏
  - 射：関手
  - 合成：関手の合成
  - 恒等射：恒等関手
]

$bold("Cat")$ は単なる例ではない。自然変換を2-射として加えれば、$bold("Cat")$ は2-圏になる。この観点は第13章で本格的に用いる。

#theorem(title: "Cat の直積閉性")[
  小圏の圏 $bold("Cat")$ は有限積を持ち、さらに任意の小圏 $cal(C), cal(D)$ に対して関手圏 $[cal(C), cal(D)]$ を指数対象として持つ。したがって $bold("Cat")$ は直積閉圏である。
]

#proof[
  直積 $cal(C) times cal(D)$ は対象と射を成分ごとに定義する。指数対象は関手圏である。自然な同型
  $
    bold("Cat")(cal(A) times cal(C), cal(D)) approx bold("Cat")(cal(A), [cal(C), cal(D)])
  $
  は、二変数関手とカリー化された関手の対応から得られる。
]

== 内部圏

圏の対象と射を集合ではなく、別の圏の対象として持つことができる。これが内部圏である。

#definition(title: "内部圏")[
  有限極限を持つ圏 $cal(E)$ における *内部圏* $C$ は、次のデータからなる。

  - 対象対象 $C_0$
  - 射対象 $C_1$
  - 始域・終域射 $s, t: C_1 -> C_0$
  - 恒等射 $e: C_0 -> C_1$
  - 合成射 $m: C_1 times_(C_0) C_1 -> C_1$

  これらは結合律と単位律を表す可換図式を満たす。

  #align(center)[
    #diagram(
      cell-size: 20mm,
      $
        C_1 times_(C_0) C_1 edge(m, ->) edge("d", pi_2, ->) & C_1 edge("d", t, ->) \
                                            C_1 edge(s, ->) & C_0
      $,
    )
  ]
]

#example(title: "内部圏の例")[
  1. $cal(E)=bold("Set")$ の内部圏は通常の小圏である。
  2. $cal(E)=bold("Top")$ の内部圏は位相圏であり、対象集合と射集合が位相空間で、構造写像が連続である。
  3. $cal(E)=bold("Grp")$ の内部圏は、群対象としての圏であり、交叉加群と深く関係する。
]

内部圏は、圏の「外側のサイズ」ではなく「内側の構造」を変化させる方法である。Hom を集合として持つ通常の圏から、空間、群、層などの中に住む圏へと視点を移す。

== 豊穣圏への入口

内部圏と並んで重要なのが豊穣圏である。内部圏が対象集合と射集合を一つの基礎圏の対象に置き換えるのに対し、豊穣圏は Hom 集合そのものを構造化する。

#definition(title: "豊穣化の考え方")[
  モノイダル圏 $cal(V)$ が与えられたとき、$cal(V)$-豊穣圏では、各対象 $X, Y$ の間の Hom は集合ではなく $cal(V)$ の対象
  $ cal(C)(X, Y) in cal(V) $
  として与えられる。
]

#example[
  距離空間は、順序集合 $([0, infinity], >=)$ をモノイダル圏と見たものに豊穣化された圏として理解できる。このとき合成律は三角不等式に対応する。
]

本章で導入したサイズ、部分圏、骨格、関手圏、内部圏という道具は、下巻の残りの議論で繰り返し用いられる。第9章では随伴から生じる内部的な代数構造としてモナドを扱い、第11章では豊穣圏を本格的に展開する。圏論の力は、抽象化を無制限に進めることではなく、抽象化の階層を正確に制御する点にある。
